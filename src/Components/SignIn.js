import React from 'react';



export const SignIn = ({handleSignIn}) => {

    

    return (
        <div>
            <button
            onClick={handleSignIn}>Sign IN</button>
        </div>
    )
}
